# ─────────────────────────────────────────────────────────────────
#  Render Blueprint – 2048 Game with AI Solver
#  Deploy via: https://render.com/docs/blueprint-spec
# ─────────────────────────────────────────────────────────────────

services:
  - type: web
    name: 2048-game
    runtime: docker
    repo: https://github.com/modaralgayal/2048-game   
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 5000
    healthCheckPath: /api/state
    autoDeploy: true